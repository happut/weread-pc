# WeRead PC v0.2.0

在 v0.1.0「能读、能翻页」的基础上，这一版把壳补成了完整的桌面客户端：**原生书架首页**、**阅读统计看板**、**书籍详情侧栏**，以及一批阅读体验打磨。

### 新增功能

- **原生书架首页**：清爽网格取代官方网页书架，本地缓存秒开；带「继续阅读」入口，书卡虚拟滚动
- **阅读统计看板**：阅读热力图、阅读时长、类型偏好、书籍 TOP5、打卡记录；数据经微信读书 Agent Gateway 实时获取
- **书籍详情侧栏**：单击书卡看元信息 / 热门书评 / 本书统计，双击直接进阅读；✕ 或 Esc 关闭
- **设置模态**：连接状态、手动 API Key 与 Cookie、缓存管理、隐私说明
- **认证升级**：优先走 Agent Gateway 自动取 Key，失败回退手动配置；Key 展示掩码

### 阅读体验

- **默认单页**：默认窗口宽 1000px，阅读器自动单页，消除双页中缝（手动拉宽仍可双页）
- **悬浮翻页钮**：右下角两个圆形上一页 / 下一页按钮，鼠标翻页更顺手
- **正文边距**：单页模式正文两侧边距收敛到 40px，版式更饱满；双页模式自动恢复网页版原版式
- **详情侧栏关闭**：关闭按钮常驻侧栏头部行内，另支持 Esc

### 安装

下载 `WeRead-PC-macOS-arm64-0.2.0.zip`，解压得到 `WeRead-PC.app`，拖进「应用程序」即可。

> ⚠️ **首次打开会被 Gatekeeper 拦截**（未做 Apple 公证）。解决方式二选一：
> - 右键点击 App → 选择「打开」→ 再点一次「打开」
> - 或在终端执行：`xattr -cr /Applications/WeRead-PC.app`

**系统要求**：macOS，Apple Silicon（arm64）。Intel 机器需要自行改用 x64 运行时。

### 从源码运行

```bash
npm install
npm start
```

### 打包

```bash
npm run build   # 产出 release/WeRead-PC-macOS-arm64-<version>.zip
```

### 已知限制

- 统计看板与详情书评依赖微信读书 Agent Gateway（需自动取 Key 成功或手动配置）
- 未做 Apple 公证，首次打开需右键绕过
- 仅提供 macOS arm64 产物
